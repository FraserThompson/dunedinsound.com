{
  "name": "dunedinsound",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "command-line-args": "^4.0.7",
    "copy": "^0.3.1",
    "fs": "0.0.2",
    "glob": "^7.1.1",
    "mkdirp": "^0.5.1",
    "sharp": "^0.18.4",
    "yamljs": "^0.2.8"
  },
  "scripts": {
    "serve": "jekyll serve",
    "dev": "jekyll serve --limit_posts=1 --incremental",
    "generate": "node ./_scripts/generate_gig.js",
    "audio": "docker-compose up --build && node ./_scripts/audio.js",
    "images": "node ./_scripts/images.js && node ./_scripts/index-media.js",
    "index-media": "node ./_scripts/index-media.js",
    "build": "set JEKYLL_ENV=production && jekyll build",
    "deploy": "s3_website push",
    "production-build": "set JEKYLL_ENV=production && jekyll build",
    "code-deploy": "set JEKYLL_ENV=production && jekyll build && s3_website push",
    "gig-deploy": "set JEKYLL_ENV=production && jekyll build && s3_website push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FraserThompson/FraserThompson.github.io.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/FraserThompson/FraserThompson.github.io/issues"
  },
  "homepage": "https://github.com/FraserThompson/FraserThompson.github.io#readme",
  "dependencies": {
    "wavesurfer.js": "^2.0.3"
  }
}
